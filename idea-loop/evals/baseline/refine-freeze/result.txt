冻结完成。

## 这一跑的实情

**环从没拿到 `pass`，它停在 plateau，是人在 plateau 处签的字。** 这件事写进了 ledger、冻结记录和 spec 的冻结摘要三处——下游读到的是「一个被人接受的状态」，不是「一个被评委判优的状态」。

评委四轮都在报同一条 P1：逐次测量的重复与字重压过那个唯一的结论。我改过两次（绿色收归一个意思、删「共 N 次」+ 历史行降重 + 结论抬到块首），它第三次、第四次原样回来，第三轮评委自己把它的 kind 从 `craft` 升成了 `pattern`。往下只剩两条路——把结论搬进组头（改那封信钉死的组头/组身分工），或给它一个 L4 响亮处理（撞 One Voice 与「反馈色永不当装饰」）。**两条都越权，我没拧，把具名问题原样留给了人。**

## 顺手挡下来的三条

- 评委建议加逐维度 delta「3.5 → 4.0」——那正是发散段变种 A 被人否掉的东西（「我要看的是上次那条评语这次还在不在，不是 3.5 变 4.0」）。评委看不到变种裁决，由我挡下。
- 减法 pass 要删组头的状态 pill——**两头都被 spec 钉死**（letter 要求组头带最近一次状态，§4.4 要求分数位就地呈现状态）。删任一头都是改结构，所以没删，当成一条 spec 级重复报上去了。
- 减法 pass 要删「题型」label——只在默认值那一帧成立，选了具体题型后就没东西说明这是个题型筛选了。

## 几处规则真的咬到了

- 第一轮减法 pass **作废重跑**：评委 P1+2×P2 却打 7，脚本算出区间 6–6。没把分数谈下来。
- Gate 2 抓到一条真的 T1 破线：「继续作答」白字落 `#137fec` 实测 **3.98:1**。改用已声明的 `#0f6fd1`（4.98:1），成了 `DESIGN.md` 里第一条带日期的 T2 判例。
- 另一条 T2 判例：反馈色在一个 surface 上只许有一个意思（绿色一度同时表示「评分结束」和「修好了」）。
- drift check 零未声明值；link sweep 五个 wikilink 全部有落点。

**§6 动效没开**，但理由不是「没有 job」——job 测试其实压中「状态」与「结果」两条，缺的是前置条件（静态构图从没拿到 `pass`）。这条分开写了，免得下游误读成「判过了不需要」。**§7 素材整段跳过**，register 是 Product。

spec 已翻 `等设计冻结` → **`在飞`**，「设计方向」被「冻结摘要」取代（不是并排）。数据契约变更列了五条，头两条——跨次引用的那条评语、以及「修好了没有」的判定——**没有真数据就没有这一页**。

还没切票就去 `/idea-loop:to-ticket`；票已经切过的话，回去补那几张的「设计冻结」字段。